<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Shop Now" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Shop Now">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-slate-50 text-slate-900 antialiased" data-controller="sidebar">
    <div class="min-h-screen">
      <div class="fixed inset-y-0 z-40 flex">
        <div class="pointer-events-none fixed inset-0 bg-slate-900/50 opacity-0 transition" data-sidebar-target="overlay" data-action="click->sidebar#close"></div>
        <aside class="fixed inset-y-0 left-0 z-50 flex w-[320px] -translate-x-full flex-col overflow-y-auto rounded-r-3xl bg-white shadow-2xl transition-transform duration-300" data-sidebar-target="panel">
          <div class="flex items-center justify-between border-b border-slate-100 px-6 py-5">
            <%= link_to root_path, class: "flex items-center gap-3 text-lg font-semibold text-slate-900" do %>
              <span class="inline-flex h-10 w-10 items-center justify-center rounded-full bg-violet-600 text-white font-bold">SN</span>
              <div class="leading-tight">
                <p>Shop Now</p>
                <p class="text-xs font-medium text-slate-500">Modern ecommerce</p>
              </div>
            <% end %>
            <button class="rounded-full border border-slate-200 p-2 text-slate-500 hover:text-slate-900" data-action="click->sidebar#close">
              ✕
              <span class="sr-only">Close menu</span>
            </button>
          </div>
          <div class="flex h-full flex-col gap-5 px-6 py-6">
            <div>
              <p class="text-xs font-semibold uppercase tracking-[0.3em] text-slate-400">Quick links</p>
              <ul class="mt-3 space-y-3 text-sm font-medium text-slate-700">
                <li><%= link_to "Track my order", cart_path, class: "flex items-center gap-2 hover:text-violet-600" %></li>
                <li><%= link_to "Launch a complaint", "#", class: "flex items-center gap-2 hover:text-violet-600" %></li>
                <li><%= link_to "Admin area", new_admin_user_session_path, class: "flex items-center gap-2 hover:text-violet-600" %></li>
              </ul>
            </div>
            <div>
              <p class="text-xs font-semibold uppercase tracking-[0.3em] text-slate-400">Categories</p>
              <ul class="mt-3 space-y-2">
                <% (@navigation_categories || []).each do |category| %>
                  <li>
                    <%= link_to products_path(category_id: category.id), class: "flex items-center justify-between rounded-2xl border border-slate-100 px-3 py-2 text-sm font-semibold text-slate-700 hover:border-violet-200 hover:text-violet-600" do %>
                      <span><%= category.name %></span>
                      <span class="text-xs font-semibold text-slate-400">→</span>
                    <% end %>
                  </li>
                <% end %>
                <% if (@navigation_categories || []).empty? %>
                  <li class="text-sm text-slate-400">No categories yet.</li>
                <% end %>
              </ul>
            </div>
            <div class="mt-auto">
              <% if user_signed_in? %>
                <div class="rounded-2xl border border-slate-100 bg-slate-50 px-4 py-4">
                  <p class="text-xs font-semibold uppercase tracking-wide text-slate-500">Signed in as</p>
                  <p class="text-lg font-semibold text-slate-900"><%= current_user.full_name %></p>
                  <div class="mt-3 flex gap-3 text-sm font-semibold">
                    <%= link_to "Account", edit_user_registration_path, class: "text-violet-600" %>
                    <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete }, class: "text-rose-500" %>
                  </div>
                </div>
              <% else %>
                <div class="rounded-2xl border border-slate-100 bg-gradient-to-br from-sky-400 to-violet-500 px-4 py-5 text-white shadow-lg">
                  <p class="text-xl font-semibold">Hello there 👋</p>
                  <p class="text-sm text-white/80">Log in to track orders, save carts, and manage wishlists.</p>
                  <div class="mt-4 flex gap-3">
                    <%= link_to "Login", new_user_session_path, class: "flex-1 rounded-full bg-white px-4 py-2 text-center text-sm font-semibold text-slate-900" %>
                    <%= link_to "Register", new_user_registration_path, class: "flex-1 rounded-full border border-white px-4 py-2 text-center text-sm font-semibold text-white" %>
                  </div>
                </div>
              <% end %>
            </div>
          </div>
        </aside>
      </div>

      <header class="fixed inset-x-0 top-0 z-30 border-b border-slate-200 bg-white">
        <div class="mx-auto flex max-w-6xl items-center gap-4 px-4 py-4">
          <button class="inline-flex h-11 w-11 items-center justify-center rounded-2xl border border-slate-200 text-slate-700 hover:border-violet-200 hover:text-violet-600" data-action="click->sidebar#toggle">
            ☰
            <span class="sr-only">Open menu</span>
          </button>
          <%= link_to root_path, class: "flex items-center gap-2 text-xl font-semibold tracking-tight text-slate-900" do %>
            <span class="inline-flex h-10 w-10 items-center justify-center rounded-full bg-violet-600 text-white font-bold">SN</span>
            <span>Shop&nbsp;Now</span>
          <% end %>
          <%= form_with url: products_path, method: :get, local: true, class: "hidden flex-1 items-center gap-2 rounded-full border border-slate-200 px-4 py-2 text-sm text-slate-600 focus-within:border-violet-500 sm:flex" do %>
            <span>🔍</span>
            <%= text_field_tag :query, params[:query], placeholder: "Search products…", class: "w-full border-0 bg-transparent text-sm focus:outline-none", autocomplete: "off" %>
          <% end %>
          <div class="ml-auto flex items-center gap-3 text-sm font-semibold text-slate-600">
            <%= link_to "Cart (#{current_cart.total_items})", cart_path, class: "rounded-full border border-slate-200 px-4 py-2 hover:border-violet-200 hover:text-violet-600" %>
            <% if user_signed_in? %>
              <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete }, class: "hidden rounded-full border border-rose-200 px-4 py-2 text-rose-600 hover:border-rose-300 sm:inline-flex" %>
            <% else %>
              <%= link_to "Sign in", new_user_session_path, class: "hidden rounded-full border border-violet-200 px-4 py-2 text-violet-600 hover:border-violet-300 sm:inline-flex" %>
            <% end %>
          </div>
        </div>
      </header>

      <main class="px-4 pb-16 pt-28 sm:pt-36">
        <div class="mx-auto flex max-w-6xl flex-col gap-6">
          <% flash.each do |key, value| %>
            <div class="flex items-center justify-between rounded-xl border px-4 py-3 text-sm <%= flash_class(key) %>" data-controller="dismiss">
              <span><%= value %></span>
              <button class="text-xs font-semibold uppercase tracking-wide" data-action="click->dismiss#hide">Close</button>
            </div>
          <% end %>
          <%= yield %>
        </div>
      </main>

      <footer class="border-t border-slate-200 bg-white/80 py-10">
        <div class="mx-auto flex max-w-6xl flex-col gap-4 px-4 text-sm text-slate-600 sm:flex-row sm:items-center sm:justify-between">
          <p>&copy; <%= Time.current.year %> Shop Now. Built with Ruby on Rails, Tailwind CSS, and PostgreSQL.</p>
          <div class="flex items-center gap-4">
            <span>Need help?</span>
            <a href="mailto:support@example.com" class="font-medium text-slate-900">support@example.com</a>
          </div>
        </div>
      </footer>
    </div>
  </body>
</html>
